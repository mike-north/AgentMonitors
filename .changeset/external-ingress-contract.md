---
'@agentmonitors/core': minor
---

Expose the versioned source-neutral external-event envelope, strict validator, canonical JSON and
semantic identity helpers, immutable limits, and stable ingest result/error types. The v1 schema is
an additive interoperability contract; durable receipt and runtime ingestion APIs follow in later
minor releases.
