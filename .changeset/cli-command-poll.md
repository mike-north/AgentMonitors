---
'@agentmonitors/cli': minor
---

Register the `command-poll` observation source and add an `agentmonitors init --type command-poll` template. `command-poll` now appears in `source list`, is accepted by `validate`, and is available to scaffold a starter monitor.
