{
  "$comment": "Reviewed exceptions for `pnpm audit --prod --audit-level high` (scripts/check-dependency-audit.mjs, Refs #290). Every entry MUST include an `expires` date (YYYY-MM-DD). An expired entry fails the gate until it is renewed (bump `expires`) or removed — this file is not a place for blanket/permanent ignores.",
  "exceptions": []
}
