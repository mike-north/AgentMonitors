---
'@agentmonitors/core': patch
'@agentmonitors/source-api-poll': patch
'@agentmonitors/source-command-poll': patch
---

Accept bare dotted keyed-collection paths such as `items` and `duration` as root-relative aliases for `$.items` and `$.duration`.
