---
'@agentmonitors/core': minor
---

Add exact-workspace receipt and object-sequence queries, plus atomic compatibility markers for
nonempty held notification batches and materialization retry outboxes.
