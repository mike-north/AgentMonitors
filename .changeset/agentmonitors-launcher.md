---
'agentmonitors': minor
---

First published release of the unscoped `agentmonitors` launcher: install the Agent Monitors CLI with `npm i -g agentmonitors`. A thin wrapper that runs `@agentmonitors/cli`.
