dist
temp
node_modules
.nx
.next
pnpm-lock.yaml
api-report

# Cursor rule files use an .mdc extension Prettier has no parser for
**/*.mdc

# Throwaway experiments are not held to the production format gate
experiments/

# The website app has its own Next.js/Markdoc toolchain (Markdoc's {% %} tags
# have no Prettier parser); excluded from the root format gate, matching its
# exclusion from build/check (nx) and eslint (eslint.config.mjs).
apps/website/

# aipm marketplace: plugin sources and the generated registries are governed by
# `aipm validate`'s freshness/byte-parity check, not Prettier. Formatting them
# would fight that check (and the bundled LICENSE has no Prettier parser).
agent-plugins/
.claude-plugin/
